# Sub-02 — Permisos

**Task:** 12 · **Tamaño:** XS

## Cambio

Catálogo (épica 08 task-02):

- `template.manage` — crear, editar metadata, eliminar plantillas. Admin ✓, Member ✗, Guest ✗.
- `template.instantiate` — crear proyectos desde plantillas. Admin ✓, Member ✓, Guest ✗.
- `template.view` — listar plantillas en settings. Admin ✓, Member ✓, Guest ✗.

## Aceptación

- [ ] Custom role con `template.instantiate` puede crear desde template pero no aparece "Save as template".
