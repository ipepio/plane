# Sub-01 — Keys i18n

**Task:** 12 · **Tamaño:** XS

## Cambio

```json
{
  "issue_properties": {
    "title": "Properties",
    "add": "Add property",
    "type": { "text": "Text", "long_text": "Long text", "number": "Number",
              "date": "Date", "boolean": "Yes / No", "select": "Select",
              "multi_select": "Multi-select", "user": "User", "url": "URL" },
    "required": "Required",
    "config": { "max_length": "Max length", "regex": "Regex", "min": "Min", "max": "Max" },
    "options": { "title": "Options", "add": "Add option" }
  }
}
```

Sync a todos los locales.

## Aceptación

- [ ] Sin strings hardcoded en componentes nuevos.
