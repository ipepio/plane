# Sub-04 — Tests CRUD

**Task:** 04 · **Tamaño:** S

## Cambio

`apps/api/plane/tests/test_issue_property.py`:
- Create property tipo `select` con `config`.
- Update `display_name`.
- List filtra por issue type.
- Destroy soft-deletes y oculta del list.
- Reorder via `relative_order` patch.

## Aceptación

- [ ] 5 tests pasan.
