# Sub-02 — `CreatePropertyModal`

**Task:** 10 · **Tamaño:** M

## Cambio

Modal step 1:
- `display_name` (autocalcula `name` slug).
- `type` (select de 9 tipos con iconos).
- `is_required` checkbox.

Submit → crea property. Si tipo es select/multi_select, abre directamente el editor de opciones (sub-04).

## Aceptación

- [ ] Crear text/number/date/boolean/user/url cierra modal y aparece en lista.
- [ ] Crear select/multi_select abre options editor.
